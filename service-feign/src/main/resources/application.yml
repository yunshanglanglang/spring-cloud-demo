eureka:
  client:
    service-url:
     defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: service-feign
  zipkin:
    base-url: http://localhost:9411/
server:
  port: 8765
feign:
  hystrix:
    enabled: true

